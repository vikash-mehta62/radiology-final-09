import React from 'react';
import ConnectionManager from '../components/connection/ConnectionManager';

const ConnectionManagerPage: React.FC = () => {
  return <ConnectionManager />;
};

export default ConnectionManagerPage;
